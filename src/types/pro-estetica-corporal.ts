/** Ligação dona ↔ tenant corporal (`pro_estetica_corporal_settings`). */
export type ProEsteticaCorporalWorkspaceLinkRow = {
  id: string
  user_id: string
  leader_tenant_id: string
  created_at: string
  updated_at: string
}
