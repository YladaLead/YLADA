/**
 * Respostas automáticas da Carol via OpenAI (webhook Z-API, admin, etc.).
 * Quando true, não há chamada à API de chat — automações de WhatsApp (Z-API) neste repo continuam.
 */
export const CAROL_AI_DISABLED = true
