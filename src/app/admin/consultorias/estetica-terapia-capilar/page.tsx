import { redirect } from 'next/navigation'

/** Rota antiga — mantida para ligações gravadas. */
export default function AdminConsultoriasEsteticaTerapiaCapilarRedirect() {
  redirect('/admin/consultorias/terapia-capilar')
}
