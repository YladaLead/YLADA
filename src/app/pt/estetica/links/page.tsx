/**
 * Coach Estética — hub canónico (Biblioteca + Meus links). Espelhado em Pro Estética corporal/capilar
 * em `.../painel/biblioteca-links` com `noAreaShell` + scopes. Só vitrine: `estetica/biblioteca/page.tsx`.
 */
import LinksHubContent from '@/components/ylada/LinksHubContent'

export default function EsteticaLinksPage() {
  return <LinksHubContent areaCodigo="estetica" areaLabel="Estética" />
}
