import YladaUsageSurveyPage from '@/components/ylada/YladaUsageSurveyPage'

/** Página pública: não exige login (evita redirect/cache de sessão). */
export default function PesquisaUsoYladaPage() {
  return <YladaUsageSurveyPage />
}
