import { redirect } from 'next/navigation'

/** Unificado com Biblioteca e links — mesmo padrão do Pro Estética Corporal. */
export default function ProEsteticaCapilarCatalogoRedirectPage() {
  redirect('/pro-estetica-capilar/painel/biblioteca-links')
}
