import { redirect } from 'next/navigation'

/** Unificado com Biblioteca e links — evita dois itens iguais no menu. */
export default function ProEsteticaCorporalCatalogoRedirectPage() {
  redirect('/pro-estetica-corporal/painel/biblioteca-links')
}
